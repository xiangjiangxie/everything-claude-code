---
description: 分析并提升测试覆盖率
agent: tdd-guide
subtask: true
---

# 测试覆盖率命令

分析测试覆盖率并识别不足之处：$ARGUMENTS

## 你的任务

1. **运行覆盖率报告**：`npm test -- --coverage`
2. **分析结果** - 识别低覆盖率区域
3. **确定优先级** - 关键代码优先
4. **生成缺失的测试** - 针对未覆盖的代码

## 覆盖率目标

| 代码类型 | 目标 |
|-----------|--------|
| 标准代码 | 80% |
| 金融逻辑 | 100% |
| 认证/安全 | 100% |
| 工具函数 | 90% |
| UI 组件 | 70% |

## 覆盖率报告分析

### 摘要
```
File           | % Stmts | % Branch | % Funcs | % Lines
---------------|---------|----------|---------|--------
All files      |   XX    |    XX    |   XX    |   XX
```

### 低覆盖率文件
[按关键程度排序的低于目标的文件]

### 未覆盖的行
[需要测试的具体行]

## 测试生成

针对每个未覆盖的区域：

### [函数/组件名称]

**位置**：`src/path/file.ts:123`

**覆盖率缺口**：[描述]

**建议的测试**：
```typescript
describe('functionName', () => {
  it('should [expected behavior]', () => {
    // Test code
  })

  it('should handle [edge case]', () => {
    // Edge case test
  })
})
```

## 覆盖率提升计划

1. **紧急**（立即添加）
   - [ ] file1.ts - 认证逻辑
   - [ ] file2.ts - 支付处理

2. **高优先级**（本迭代添加）
   - [ ] file3.ts - 核心业务逻辑

3. **中等优先级**（修改文件时添加）
   - [ ] file4.ts - 工具函数

---

**重要**：覆盖率是一个指标，而非目标。专注于有意义的测试，而不仅仅是追求数字。
