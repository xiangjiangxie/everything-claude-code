# 循环状态命令

检查活跃循环的状态、进度和失败信号。

## 用法

`/loop-status [--watch]`

## 报告内容

- 活跃的循环模式
- 当前阶段和最后成功的检查点
- 失败的检查（如有）
- 预估的时间/成本偏差
- 推荐的干预措施（继续/暂停/停止）

## 监视模式

当指定 `--watch` 时，定期刷新状态并显示状态变化。

## 参数

$ARGUMENTS:
- `--watch` 可选
