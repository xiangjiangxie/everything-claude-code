---
description: 从 ~/.claude/session-data/ 加载最近的会话文件，并从上次会话结束的地方恢复工作，保留完整上下文。
---

# 恢复会话命令

加载上次保存的会话状态，在进行任何工作之前完全了解情况。
此命令是 `/save-session` 的配套命令。

## 何时使用

- 开始新会话以继续前一天的工作
- 由于上下文限制而开始新会话后
- 从其他来源接收到会话文件时（只需提供文件路径）
- 任何时候你有会话文件并希望 Claude 在继续之前完全吸收它

## 用法

```
/resume-session                                                      # 加载 ~/.claude/session-data/ 中最近的文件
/resume-session 2024-01-15                                           # 加载该日期最近的会话
/resume-session ~/.claude/session-data/2024-01-15-abc123de-session.tmp  # 加载当前 short-id 格式的会话文件
/resume-session ~/.claude/sessions/2024-01-15-session.tmp               # 加载特定的旧格式文件
```

## 流程

### 步骤 1：查找会话文件

如果未提供参数：

1. 检查 `~/.claude/session-data/`
2. 选择最近修改的 `*-session.tmp` 文件
3. 如果文件夹不存在或没有匹配的文件，告诉用户：
   ```
   在 ~/.claude/session-data/ 中未找到会话文件
   在会话结束时运行 /save-session 来创建一个。
   ```
   然后停止。

如果提供了参数：

- 如果看起来像日期（`YYYY-MM-DD`），先搜索 `~/.claude/session-data/`，然后搜索旧版
  `~/.claude/sessions/`，查找匹配 `YYYY-MM-DD-session.tmp`（旧格式）或
  `YYYY-MM-DD-<shortid>-session.tmp`（当前格式）的文件，
  并加载该日期最近修改的版本
- 如果看起来像文件路径，直接读取该文件
- 如果未找到，清楚地报告并停止

### 步骤 2：读取整个会话文件

读取完整文件。暂时不要总结。

### 步骤 3：确认理解

以此精确格式回复结构化简报：

```
会话已加载：[文件的实际解析路径]
════════════════════════════════════════════════

项目：[文件中的项目名称/主题]

正在构建的内容：
[用你自己的话写 2-3 句摘要]

当前状态：
✅ 有效：[数量] 个已确认项目
🔄 进行中：[列出进行中的文件]
🗒️ 未开始：[列出已规划但未触及的]

不要重试的内容：
[列出每个失败的方法及其原因 — 这很关键]

未解决的疑问/阻塞问题：
[列出任何阻塞问题或未回答的疑问]

下一步：
[如果文件中定义了确切的下一步]
[如果未定义："未定义下一步 — 建议在开始前一起审查'尚未尝试的'部分"]

════════════════════════════════════════════════
准备继续。你想做什么？
```

### 步骤 4：等待用户

不要自动开始工作。不要触及任何文件。等待用户说下一步做什么。

如果会话文件中明确定义了下一步，且用户说"继续"或"是"或类似的话 — 执行那个确切的下一步。

如果没有定义下一步 — 询问用户从哪里开始，并可选地从"尚未尝试的"章节建议一个方法。

---

## 边界情况

**同一日期有多个会话**（`2024-01-15-session.tmp`、`2024-01-15-abc123de-session.tmp`）：
加载该日期最近修改的匹配文件，无论它使用旧版无 ID 格式还是当前 short-id 格式。

**会话文件引用了不再存在的文件：**
在简报中注明 — "⚠️ `path/to/file.ts` 在会话中被引用但在磁盘上未找到。"

**会话文件是 7 天前的：**
注明时间间隔 — "⚠️ 此会话来自 N 天前（阈值：7 天）。情况可能已发生变化。" — 然后正常继续。

**用户直接提供文件路径（例如从队友转发的）：**
读取它并遵循相同的简报流程 — 无论来源如何，格式是相同的。

**会话文件为空或格式错误：**
报告："找到会话文件但看起来为空或无法读取。你可能需要使用 /save-session 创建一个新的。"

---

## 示例输出

```
会话已加载：/Users/you/.claude/session-data/2024-01-15-abc123de-session.tmp
════════════════════════════════════════════════

项目：my-app — JWT 认证

正在构建的内容：
使用存储在 httpOnly cookie 中的 JWT 令牌的用户认证。
注册和登录端点已部分完成。通过中间件的路由保护
尚未开始。

当前状态：
✅ 有效：3 个项目（注册端点、JWT 生成、密码哈希）
🔄 进行中：app/api/auth/login/route.ts（令牌可用，cookie 尚未设置）
🗒️ 未开始：middleware.ts、app/login/page.tsx

不要重试的内容：
❌ Next-Auth — 与自定义 Prisma 适配器冲突，每次请求都抛出适配器错误
❌ 用 localStorage 存储 JWT — 导致 SSR hydration 不匹配，与 Next.js 不兼容

未解决的疑问/阻塞问题：
- cookies().set() 在 Route Handler 中有效还是仅在 Server Actions 中有效？

下一步：
在 app/api/auth/login/route.ts 中 — 使用
cookies().set('token', jwt, { httpOnly: true, secure: true, sameSite: 'strict' })
将 JWT 设置为 httpOnly cookie，
然后用 Postman 测试响应中的 Set-Cookie 头。

════════════════════════════════════════════════
准备继续。你想做什么？
```

---

## 注意事项

- 加载会话文件时永远不要修改它 — 它是只读的历史记录
- 简报格式是固定的 — 即使章节为空也不要跳过
- "不要重试的内容"必须始终显示，即使只是说"无" — 这太重要了不能遗漏
- 恢复后，用户可能希望在新会话结束时再次运行 `/save-session` 来创建新的日期文件
